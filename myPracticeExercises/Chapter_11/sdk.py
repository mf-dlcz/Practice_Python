#                       SDK - Software Development Kit

'''
                NOTES:

SDK:    A software development kit, or SDK, is a package of tools that developers can use 
        to create applications for specific platforms, operating systems, or devices.

There are two primary ways to interact with AWS services:
The client (low level) API
The resource (high level) API

- The client API provides one-to-one mappings to the underlying
HTTP API operations.

- The client API provides direct access to the AWS service APIs.
'''